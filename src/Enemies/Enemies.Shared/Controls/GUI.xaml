<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:Enemies.Controls;assembly=Enemies.MonoGame"
             x:Class="Enemies.Controls.GUI">
    <Grid VerticalOptions="Fill" HorizontalOptions="Fill">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="150"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controls:ImageButton OnClick="AddPlayer_Click" Grid.Row="0" Grid.Column="1" Text="Player" TextColor="Black" ImageNormal="GUI/cursor_closed" ImageOver="GUI/cursor_pointer" VerticalOptions="Center" HorizontalOptions="Center" />
        <controls:ImageButton OnClick="AddEnemy_Click" Grid.Row="1" Grid.Column="1" Text="Enemy" TextColor="Black" ImageNormal="GUI/cursor_closed" ImageOver="GUI/cursor_pointer" VerticalOptions="Center" HorizontalOptions="Center"/>
        <controls:ImageButton x:Name="AddObjective" OnClick="AddObjective_Click" Grid.Row="2" Grid.Column="1" Text="Objective" TextColor="Black" ImageNormal="GUI/cursor_closed" ImageOver="GUI/cursor_pointer" VerticalOptions="Center" HorizontalOptions="Center"/>
    </Grid>
</ContentPage>